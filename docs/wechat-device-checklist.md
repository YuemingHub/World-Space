# 微信真机验证清单（待 Founder 设备执行）

> 本地环境无微信真机，以下各项无法验证，页面保持诚实标注（未验证的不写死结论）。
> 每项给出：怎么测 / 预期 / 如果不符怎么办。验证完把结果记在这份文件里。

最后更新：2026-09-12（分支 `agent/world-space-continuous-local-20260911` = 0b7f3dd 之后）

## 1. 微信内打开 ymai.fun 的外跳行为

- **怎么测**：微信里打开 https://ymai.fun → 点任意「打开豆包 →」按钮。
- **预期**：在微信内置浏览器里打开豆包（同窗口跳转或半屏浮层），返回手势能回到 ymai.fun。
- **如果不符**：若外跳后无法返回或页面丢失，把按钮改为提示「长按二维码/复制链接到浏览器打开」，不猜具体写法，实测后定。

## 2. 复制按钮在微信里的降级路径

- **怎么测**：微信内打开任意目标面板 → 点「复制」。
- **预期**：按钮显示「已复制 ✓」，可粘贴。
- **依据**：桌面 Chromium 已模拟 clipboard API 被拒场景，execCommand 兜底实测通过（2026-09-11）；真机微信 webview 是最后的确认。
- **如果不符**：按钮会如实显示「没复制上，长按文字复制」——这是设计好的诚实兜底，只需确认文案出现而非谎报成功。

## 3. 深链在微信里分享

- **怎么测**：把 `https://ymai.fun/#write` 发给另一台手机（或文件传输助手）再点开。
- **预期**：页面打开后「写点东西」面板自动展开。
- **依据**：浏览器实测真加载后自动展开通过；风险仅在微信 WebView 的链接预处理剥掉 hash。
- **如果不符**：若微信剥 hash，考虑分享文案带「打开后点【写点东西】」的指引（一句话，不改结构）。

## 4. 豆包 / DeepSeek 登录流程（页面提示是否够用）

- **怎么测**：未登录状态点「打开豆包 →」和「打开 DeepSeek →」，按页面提示（手机号或微信登录，免费）走通一次。
- **预期**：登录后能直接对话；页面文案与实际登录界面用词一致（「手机号或微信登录」）。
- **如果不符**：登录方式或用词不一致时改页面提示语，保持与实际界面一字不差。

## 5. 通义听悟上传录音真机流程

- **怎么测**：手机浏览器打开 tingwu.aliyun.com → 登录 → 上传一段真实录音 → 确认转文字+总结效果与免费额度显示。
- **预期**：与官方计费文档一致（新用户免费 90 天、文件转写每天免费 2 小时）。
- **如果不符**：额度或门槛变了 → 按实况改整理面板文案；若免费层取消 → 触发 goal-coverage 重开（会议录音行退回 UNSERVED）。

## 6. iOS 16 以下「重复项目」缺失实测

- **怎么测**：找一台 iOS 15 及以下的 iPhone → 照片 App 找「精选集/重复项目」。
- **预期**：确实没有该精选集（页面已如实拆分：刚升级等一晚才出现；更老系统先升级或手动删）。
- **如果不符**：若老系统也有入口，改照片面板措辞。

## 7. 豆包 App 语音输入（不会打字路径）

- **怎么测**：装豆包 App → 不打字，点话筒按住说话。
- **预期**：语音转文字后正常回答（公开报道核证过功能存在，未真机验证）。
- **如果不符**：入口名称与「话筒」不符时，按实际界面改三步路径里那句话。
